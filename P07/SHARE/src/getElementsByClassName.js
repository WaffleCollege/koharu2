/* exported getElementsByClassName */

/**
 * getElementByClassName: クラスclassNameを持つ要素を取得
 * @param {string} className 
 * @returns {Array}
 */


const getElementsByClassName = (className) => {
  let out = []; //出力用配列の初期化
  
  
}
